import type { NextConfig } from "next";

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["uibqxvcoxrpxilrjhmon.supabase.co"], // 👈 add your Supabase domain here
  },
};

export default nextConfig;
